// var router = require("express").Router();
// var connection = require("../db/connection");

// // GET route that finds all tables with isWaiting = false and sends back as JSON
// // app.get("/api/home", function(req, res) {

// //   connection.query("SELECT * FROM tables WHERE isempty = false", (err, Data) => {

// //     if (err) {
// //       console.log(err);
// //       return res.status(500).end();
// //     }
// //     // send array of tables back
// //     res.json(Data);

// //   });

// // });
// router.post("/api/tables", function(req, res) {

//   connection.query("SELECT * FROM tables WHERE isempty = false", function(err, Data){
    
//   })

// })
